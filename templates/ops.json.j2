[
{% for player in ops_users %}
  {
    "uuid": "{{ player.uuid }}",
    "name": "{{ player.name }}",
    "level": 4,
    "bypassesPlayerLimit": false
  }{% if not loop.last %},{% endif %}
{% endfor %}
]
